import numpy as np
import random as rng
import matplotlib.pyplot as plt
#from planar_utils import plot_decision_boundary, sigmoid, load_planar_dataset

# Don't I need planar_utils? How to install it? It's a local file included in the zip.


# Raw pixles, just linearize image.
# HoG, use package to extract features.
# LBP,


def forward_propagation(data, parameters):

    print("Starting forward propagation.")


def compute_cost(A2, labels, parameters):
    print("Computing cost.")


def backwards_propagation(parameters, cache, data, labels):
    print("Starting backwards propagation.")


def update_parameters(parameters, gradients, learning_rate = 1.2):
    print("Updating parameters.")


def nn_model(data, labels, hidden_layers, iterations):
    print("Building neural network.")

